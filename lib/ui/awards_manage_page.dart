import 'package:flutter/material.dart';

class AwardsManagePage extends StatelessWidget {
  const AwardsManagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
